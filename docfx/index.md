# Klassd API reference

Auto-generated reference for the public Klassd API, extracted from the source XML
documentation comments.

> [!TIP]
> Looking for **guides and a quickstart**? See the [main documentation](/docs). This section
> is the type-by-type API reference.

## Where to start

- **Content types & attributes** — `Klassd.Core.Abstractions` (`PageBase`, `BlockBase`,
  `[CmsPage]`, `[CmsField]`, `[AllowedChildren]`, `[AllowedRelations]`, `PageReference`,
  `MediaReference`).
- **Property types** — `Klassd.Core.PropertyTypes` (`IPropertyType`, the defaults, and the
  `[PropertyEditor]` discovery seam).
- **Registries & services** — `Klassd.Core.Services` (page/block/property-type registries).
- **The engine & delivery** — `Klassd.Backoffice` (`AddKlassd`/`UseKlassd`, the headless `/api`).
- **Storage contracts** — `Klassd.Abstractions` (`IPageStore`, `IMediaStore`, `IUnitOfWork`,
  the DB-agnostic record POCOs).

Use the **API** link in the top navigation or the search box to jump to a specific type.
