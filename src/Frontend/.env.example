# Base URL of the Klassd CMS delivery API.
# Leave empty in dev (Vite proxies /api → http://localhost:5080) and in prod when a
# reverse proxy serves the site and CMS under one origin. Otherwise set the CMS origin:
# VITE_CMS_BASE=https://cms.getklassd.com
VITE_CMS_BASE=
